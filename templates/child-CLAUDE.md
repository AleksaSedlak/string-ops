# <name>

<one sentence: what this repository is for>

## Working here

- Install: `<command>`
- Test: `<command>`
- Lint: `<command>`
- Run locally: `<command>`

## Rules

- Work on the branch you were given; never commit to a protected branch.
- One commit per task, message in the imperative, no references to tools or assistants.
- Every change to behaviour comes with a test next to it.
- Do not add a dependency the task did not name.

## Layout

<three to six lines: the folders that matter and what lives in each. Keep this a map, not an essay; instructions above are what an agent follows, descriptions are what it skims.>

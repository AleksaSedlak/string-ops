# Routing vocabulary

Words people use for each area of this system and the repos those words usually mean. `/route` reads this table first and `REPOS.md` second. `/start` writes the first version from the registry; edit it whenever a request was routed to the wrong place.

| Area | Words in the request | Usual repos |
|---|---|---|
<!-- one row per area, for example:
| web | dashboard, browser, chart page | web-app, api |
| billing | subscription, invoice, checkout | billing, api |
-->
